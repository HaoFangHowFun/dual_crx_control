diff --git a/fanuc_hardware_interface/include/fanuc_robot_driver/hardware_interface.hpp b/fanuc_hardware_interface/include/fanuc_robot_driver/hardware_interface.hpp
index 9eb33d0..1ea6434 100644
--- a/fanuc_hardware_interface/include/fanuc_robot_driver/hardware_interface.hpp
+++ b/fanuc_hardware_interface/include/fanuc_robot_driver/hardware_interface.hpp
@@ -8,6 +8,7 @@
 #include <atomic>
 #include "fanuc_client/fanuc_client.hpp"
 #include "fanuc_client/gpio_buffer.hpp"
+#include "fanuc_client/latency_trace.hpp"
 #include "fanuc_robot_driver/constants.hpp"
 #include "gpio_config/gpio_config.hpp"
 #include "hardware_interface/handle.hpp"
@@ -60,6 +61,7 @@ public:
   hardware_interface::return_type write(const rclcpp::Time& time, const rclcpp::Duration& period) override;
 
 private:
+  fanuc_latency::Trace latency_trace_{"hardware"};
   struct RobotStatusValues
   {
     // Unless otherwise stated, all values are bools.
diff --git a/fanuc_hardware_interface/src/hardware_interface.cpp b/fanuc_hardware_interface/src/hardware_interface.cpp
index bc794f1..1f27c11 100644
--- a/fanuc_hardware_interface/src/hardware_interface.cpp
+++ b/fanuc_hardware_interface/src/hardware_interface.cpp
@@ -686,6 +686,7 @@ hardware_interface::return_type FanucHardwareInterface::read(const rclcpp::Time&
     {
       fr_joint_pos_[i] = M_PI / 180.0 * joint_angles[i];
     }
+    latency_trace_.record("hardware_read", fr_joint_pos_);
     if ((fr_prev_joint_pos_.array() != fr_joint_pos_.array()).any())
     {
       const double dt = static_cast<double>(fanuc_client_->getControlPeriod()) / 1000.0;
@@ -765,6 +766,7 @@ hardware_interface::return_type FanucHardwareInterface::write(const rclcpp::Time
 
   try
   {
+    latency_trace_.record("hardware_write", joint_targets_);
     joint_targets_degrees_.array() = 180.0 / M_PI * joint_targets_.array();
     fanuc_client_->writeJointTarget(joint_targets_degrees_);
 
diff --git a/fanuc_libs/fanuc_client/src/fanuc_client.cpp b/fanuc_libs/fanuc_client/src/fanuc_client.cpp
index 514a095..ab07187 100644
--- a/fanuc_libs/fanuc_client/src/fanuc_client.cpp
+++ b/fanuc_libs/fanuc_client/src/fanuc_client.cpp
@@ -13,6 +13,7 @@
 #include <utility>
 
 #include "fanuc_client/gpio_buffer.hpp"
+#include "fanuc_client/latency_trace.hpp"
 #include "readerwriterqueue.h"
 #include "stream_motion/packets.hpp"
 
@@ -72,6 +73,9 @@ constexpr ContactStopMode ToContactStopMode(stream_motion::ContactStopStatus sta
 
 struct FanucClient::PQueueImpl
 {
+  explicit PQueueImpl(const std::string& ip) : enqueue_trace(ip + "_enqueue"), stream_trace(ip + "_stream") {}
+  fanuc_latency::Trace enqueue_trace;
+  fanuc_latency::Trace stream_trace;
   using StampedEigen = std::tuple<std::chrono::duration<double>, Eigen::VectorXd>;
   // TODO: Consider merging the command and command_io queues.
   moodycamel::BlockingReaderWriterQueue<StampedEigen> command_queue_;
@@ -94,7 +98,7 @@ FanucClient::FanucClient(std::string robot_ip, const uint16_t stream_motion_port
                          RMISingleton::setRMIInstance(std::move(rmi_connection_interface)) }
   , out_cmd_interp_buff_target_{ 8 }
   , force_sensor_type_{ 0 }
-  , p_queue_impl_(std::make_unique<PQueueImpl>())
+  , p_queue_impl_(std::make_unique<PQueueImpl>(robot_ip_))
 {
   if (encoding != "")
   {
@@ -235,6 +239,8 @@ void FanucClient::writeJointTarget(const Eigen::VectorXd& joint_targets)
     throw std::invalid_argument("Joint targets size does not match the size of last joint angles.");
   }
   auto cur_time_from_start = std::chrono::high_resolution_clock::now() - start_time_;
+  p_queue_impl_->enqueue_trace.record("client_enqueue", last_joint_angles_cmd_, M_PI / 180.,
+                                      p_queue_impl_->command_queue_.size_approx());
   p_queue_impl_->command_queue_.enqueue({ cur_time_from_start, last_joint_angles_cmd_ });
 }
 
@@ -331,6 +337,9 @@ void FanucClient::streamMotionThread(const Eigen::VectorXd& joint_angles)
       }
     }
 
+    p_queue_impl_->stream_trace.record("status_return", status.joint_angle, M_PI / 180.,
+        NAN, NAN, NAN, status.sequence_no, status.time_stamp, status.safety_scale);
+
     // set estimated time to first command's timestamp
     if ((dev_time == 0.0) && (p_queue_impl_->command_queue_.size_approx() != 0))
     {
@@ -392,7 +401,17 @@ void FanucClient::streamMotionThread(const Eigen::VectorXd& joint_angles)
     // Handle IO commands.
     while (p_queue_impl_->command_io_queue_.try_dequeue(command_io)) {}
 
+    const bool trace_enabled = p_queue_impl_->stream_trace.enabled();
+    const double send_start = trace_enabled ? fanuc_latency::monotonic_now() : 0.;
+    const double represented_age = trace_enabled ?
+        std::chrono::duration<double>(std::chrono::high_resolution_clock::now() - start_time_).count() -
+        (alpha * command_timestamp + (1. - alpha) * last_command_timestamp) : NAN;
+    p_queue_impl_->stream_trace.record("send_begin", command_pos, M_PI / 180., size_before,
+        represented_age, alpha, status.sequence_no, status.time_stamp, status.safety_scale);
     stream_motion_->sendCommand(command_pos, !is_streaming_, command_io, ((motion_possible && do_motn_ctrl_) ? 1 : 0));
+    p_queue_impl_->stream_trace.record("send_end", command_pos, M_PI / 180., size_before,
+        represented_age, alpha, status.sequence_no, status.time_stamp, status.safety_scale,
+        trace_enabled ? fanuc_latency::monotonic_now() - send_start : NAN);
     p_queue_impl_->robot_state_queue_.enqueue(status);
   }
 }
diff --git a/fanuc_libs/fanuc_client/include/fanuc_client/latency_trace.hpp b/fanuc_libs/fanuc_client/include/fanuc_client/latency_trace.hpp
new file mode 100644
--- /dev/null
+++ b/fanuc_libs/fanuc_client/include/fanuc_client/latency_trace.hpp
@@ -0,0 +1,96 @@
+#pragma once
+// Optional diagnostic only. One producer per Trace; flush only after it stops.
+#include <array>
+#include <algorithm>
+#include <cmath>
+#include <cstdio>
+#include <cstdlib>
+#include <fstream>
+#include <iomanip>
+#include <limits>
+#include <string>
+#include <vector>
+#include <time.h>
+#include <unistd.h>
+
+namespace fanuc_latency
+{
+inline double monotonic_now() noexcept
+{
+  timespec ts{};
+  clock_gettime(CLOCK_MONOTONIC, &ts);
+  return static_cast<double>(ts.tv_sec) + static_cast<double>(ts.tv_nsec) * 1e-9;
+}
+
+class Trace
+{
+  struct Row
+  {
+    double time{};
+    const char* stage{};
+    std::array<double, 6> q{};
+    double depth{}, age{}, alpha{}, sequence{}, controller_stamp{}, scaling{}, duration{};
+  };
+  std::vector<Row> rows_;
+  std::string path_;
+  size_t count_ = 0;
+
+public:
+  explicit Trace(const std::string& label) noexcept
+  {
+    const char* directory = std::getenv("FANUC_LATENCY_DIR");
+    if (!directory || !*directory) return;
+    try
+    {
+      // Allocate and touch storage before the realtime loop. Last 120 s at 1500 rows/s.
+      rows_.resize(180000);
+      path_ = std::string(directory) + "/" + label + "_" + std::to_string(getpid()) +
+              "_" + std::to_string(static_cast<long long>(monotonic_now()*1e9)) + ".csv";
+    }
+    catch (...) { rows_.clear(); }
+  }
+  Trace(const Trace&) = delete;
+  Trace& operator=(const Trace&) = delete;
+  bool enabled() const noexcept { return !rows_.empty(); }
+
+  template<class Values>
+  void record(const char* stage, const Values& q, double unit_scale = 1.,
+              double depth = NAN, double age = NAN, double alpha = NAN,
+              double sequence = NAN, double controller_stamp = NAN,
+              double scaling = NAN, double duration = NAN) noexcept
+  {
+    if (!enabled()) return;
+    Row& row = rows_[count_++ % rows_.size()];
+    row.time = monotonic_now();
+    row.stage = stage;
+    for (size_t i=0; i<6; ++i) row.q[i] = i < static_cast<size_t>(q.size()) ? q[i]*unit_scale : NAN;
+    row.depth=depth; row.age=age; row.alpha=alpha; row.sequence=sequence;
+    row.controller_stamp=controller_stamp; row.scaling=scaling; row.duration=duration;
+  }
+
+  ~Trace() noexcept
+  {
+    if (!enabled() || !count_) return;
+    try
+    {
+      std::ofstream out(path_);
+      if (!out) { std::fprintf(stderr, "Latency trace cannot open %s\n", path_.c_str()); return; }
+      out << "time_s,stage,J1_rad,J2_rad,J3_rad,J4_rad,J5_rad,J6_rad,queue_depth,represented_age_s,alpha,controller_sequence,controller_stamp_raw,scaling,call_duration_s\n";
+      out << std::setprecision(17);
+      const size_t start = count_ > rows_.size() ? count_ - rows_.size() : 0;
+      for (size_t i=start; i<count_; ++i)
+      {
+        const Row& r = rows_[i % rows_.size()];
+        out << r.time << ',' << r.stage;
+        for (double q : r.q) out << ',' << q;
+        out << ',' << r.depth << ',' << r.age << ',' << r.alpha << ',' << r.sequence
+            << ',' << r.controller_stamp << ',' << r.scaling << ',' << r.duration << '\n';
+      }
+      if (!out) std::fprintf(stderr, "Latency trace write failed: %s\n", path_.c_str());
+      std::fprintf(stderr, "Latency trace: %s (%zu rows, %zu overwritten)\n", path_.c_str(),
+                   std::min(count_, rows_.size()), start);
+    }
+    catch (...) { std::fprintf(stderr, "Latency trace save failed\n"); }
+  }
+};
+}  // namespace fanuc_latency
